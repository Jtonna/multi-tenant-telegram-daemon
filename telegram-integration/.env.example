# Telegram Bot Token (get from @BotFather on Telegram)
BOT_TOKEN=your_token_here

# Chat Router URL (optional — omit to run in standalone echo mode)
CHAT_ROUTER_URL=http://localhost:3100

# Access Control — Allowlists (comma-separated Telegram IDs)
# Use the /config command in Telegram to discover your user ID and group chat ID
# Empty values = deny all (default-deny). Bot is locked down unless IDs are listed.

# Comma-separated Telegram user IDs allowed to send DMs to the bot
TELEGRAM_ALLOWED_USER_IDS=123456789

# Comma-separated Telegram group chat IDs where the bot will respond
TELEGRAM_ALLOWED_GROUP_IDS=-1001234567890
